import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
export const adminSlice = createSlice({
  name: "ui",
  initialState,
  reducers: {},
  extraReducers: (builder) => {
    //   builder.addCase(,()=>{})
  },
});
